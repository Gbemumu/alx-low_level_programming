#include "main.h"
/**
 * _abs - prints the absolute of an int
 *
 * @n: int to be checked
 *
 * Return: -n if n is negative and n if n is positive
 */
int _abs(int n)
{
	if (n < 0)
		return(-n);
	else
		return (n);
}
