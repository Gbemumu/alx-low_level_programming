#include "main.h"

/**
 * mul - mutiples two ints
 *
 * @a: one of the ints to be multiplied
 *
 * @b: the second int
 *
 * Return: Returns the result
 */
int mul(int a, int b)
{

	int result;

	result = a * b;

	return (result);
}
